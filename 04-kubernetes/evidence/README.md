# Evidence

- `netpol-before.txt` — curl from tenant-b to tenant-a succeeding
- `netpol-after.txt` — the same curl timing out after default-deny
- `psa-rejection.txt` — the admission controller rejecting the privileged pod
- `kube-bench.txt` — CIS Kubernetes Benchmark results
