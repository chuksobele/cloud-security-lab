kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
networking:
  # Disable the default CNI so Calico can enforce NetworkPolicy.
  disableDefaultCNI: true
  podSubnet: "192.168.0.0/16"
nodes:
  - role: control-plane
  - role: worker
